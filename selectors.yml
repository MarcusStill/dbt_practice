selectors:
  - name: incremental_models
    description: "Update all incremental models"
    definition:
      method: config
      value: materialized
      arguments:
        config: incremental

# dbt run --selector incremental_models